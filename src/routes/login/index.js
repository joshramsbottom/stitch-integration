import { h } from "preact";
import LoginButton from "../../components/login-button";

export default function Login() {
  return (
    <div>
      <LoginButton>Log in with Stitch</LoginButton>
    </div>
  );
}
